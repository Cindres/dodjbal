{
  "name": "dodjball",
  "version": "0.1.0",
  "description": "Attempt at being productive.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:prod": "npm install --production && webpack -p --config webpack.prod.config.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "copy-webpack-plugin": "^4.0.1",
    "phaser": "^2.6.2",
    "webpack": "^2.2.1"
  },
  "devDependencies": {
    "webpack-dev-server": "^2.3.0"
  }
}
